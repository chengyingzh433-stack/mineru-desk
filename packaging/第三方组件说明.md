# 第三方组件与分发说明

本包是 MinerU 的非官方桌面封装，不代表 OpenDataLab 官方发布。

0.3.1 对 fasttext/FastText.py 的 load_model 增加 Windows Unicode 路径兼容处理：原路径加载失败时，仅在当前工作目录创建 ASCII 相对路径临时副本，加载后立即清理，不改当前目录、不改原模型；原 FastText MIT 版权与许可保留。这是 MinerU Desk 的修改，并非上游原版行为。

- MinerU 3.4.5：`licenses/MinerU-LICENSE.md`；原有代码与许可保留在 `runtime/Lib/site-packages/mineru*`。
- Python 3.12.10：使用 [Python 官方 embeddable 发行包](https://www.python.org/downloads/release/python-31210/)，许可在 `licenses/Python-LICENSE.txt` 和 `runtime/LICENSE.txt`。
- PyTorch 2.8.0+cpu 及其他 Python 依赖：版本/许可元数据在 `dependency-inventory.json`，原始许可证保留在各包和 `*.dist-info/licenses` 中。
- Electron、Chromium 与 npm 依赖：许可见 `app/LICENSE*`、`app/resources/app/node_modules`。可读前端/任务服务源代码随包保留。
- 本安装包不包含 Pipeline/VLM 大模型；运行依赖自身附带的小型辅助资源保留不动。界面下载的模型遵循各自上游许可，不能直接套用 MinerU 程序的许可。

MinerU 当前采用 Apache 2.0 加附加条款，并非无条件的通用 Apache 授权；大规模商业使用和对外在线服务有额外要求，请阅读保留的完整许可。[上游许可](https://github.com/opendatalab/MinerU/blob/master/LICENSE.md)

这份清单用于保留来源，不代替对具体商业分发方式的许可审查。不要去掉各组件的版权、许可和署名文件。

Python 版本为复用并验证现有依赖而固定，不是最新安全补丁版本；不应把本服务开放到公网。更新随包运行时需要重新进行兼容性与安全检查，不能只替换一个 Python EXE。
