# MinerU Desk · 使用说明

版本：0.3.1。基于 OpenDataLab MinerU 的非官方 Windows 桌面客户端，独立于其他论文工作流。解析能力来自上游，来源说明见 [第三方声明](THIRD_PARTY_NOTICES.md)。

安装版用户先阅读安装目录的「开始使用.md」和「给Codex的接入说明.md」。安装版内置 CPU 运行环境，不含 Pipeline/VLM 大模型。工作区默认在程序目录的父目录下，名为 `MinerU-Desk-Data`；模型、缓存、结果可另选路径。不要在随包 Python 中直接升级依赖。旧便携版的数据位置以随包说明为准。

## 存储、进度与配置建议

### 模型与缓存放到其他磁盘

“模型与服务”顶部可直接选择模型保存地址，选择后立即保存，不再需要额外点击总设置页的保存按钮。下载前再次显示模型目录和临时目录。

“存储与清理”分别设置模型目录、缓存与运行环境目录和结果目录。建议使用其他磁盘的专用子目录，例如 `D:\MinerU\模型`、`D:\MinerU\缓存`、`D:\MinerU\转换结果`。手动输入路径后点击“保存路径”。

通过本应用启动的转换、下载、服务和安装，其系统临时目录、Hugging Face / ModelScope 模型缓存、pip 下载缓存、Torch / XDG 缓存会重定向到这些位置。新安装的环境放到缓存目录下的 `runtime`；已安装的 Python 环境不会被搬迁，任务记录与加密 Token 等小文件仍保留在应用数据目录。第三方程序不遵循这些环境变量产生的文件、直接在命令行运行的 MinerU 不受本应用完全控制。

修改路径只影响以后提交的任务。已有队列使用提交时的设置；以前的模型和成果不自动迁移或删除。离线运行仍需关联对应的模型目录。空间页保留曾经登记的旧缓存位置，并显示实际使用的本地环境位置。

### 一键清理的边界

点击“一键安全清理”会先扫描并列出准确路径和体积，确认后永久删除；不是无提示地清空磁盘。

- 默认选择：应用登记的已结束任务临时文件、已知未完成模型片段（`.incomplete` / `._____temp`）、应用专用 pip 下载缓存。清理下载片段会失去对应片段的续传能力。
- 单独确认：失败或取消任务的输出残留，可能含有可用片段，不默认勾选。
- 保留：原始文档、已关联模型、正常转换成果及图片、编辑文件、来源不明的文件、其他工具的全局缓存。未能可靠判断的完整模型缓存不会因为“没有配置”就被删除，可在模型页人工管理。
- 转换、下载、安装、受管理服务运行时拒绝清理。预览后文件或引用发生变化时，要求重新扫描；不会跟随目录联接去删外部文件。
- 成功任务默认自动清理本次专用临时目录；失败残留可稍后扫描。可关闭自动清理，或设置启动前最低磁盘余量阈值。

清理记录保存时间、实际释放量、具体路径和失败原因。文件被其他程序占用时可能部分失败，不能把预计大小当作已经释放的空间。目录统计可能互相包含，不应把所有卡片简单相加。应用不提供回收站恢复功能。

### 进度条与图片

模型和文件任务的进度条取自后端日志中的真实阶段百分比、页数或字节数；百分比通常针对当前权重文件、当前 OCR / 页面处理阶段，不是整个模型组或整个流程。阶段切换时百分比可以回落。没有数字时显示不定进度，不估算剩余时间。日志变化时更新，后台长时间不输出不会出现虚构进度。

本地结果包保留 Markdown 引用的图片，预览会加载同目录下的图片资源；不要只复制单个 Markdown 而遗漏图片文件夹。Markdown 表达阅读顺序，不复刻 PDF 的分页、双栏和坐标。自动判断是本应用默认方法；强制 OCR 不是删除图片的开关，但识别错误仍可能造成漏图或错误裁剪。图片 / 图表理解是额外内容分析，与保存插图不同。重要图表与公式始终对照原 PDF；见[官方输出结构](https://opendatalab.github.io/MinerU/reference/output_files/)。

### 配置与建议

打开左侧“配置与建议”会自动检测 CPU、系统可用物理内存总量、显卡型号、所选 MinerU 的 Python / PyTorch 与可验证的 CUDA 专用显存。检测失败显示未知；核显共享内存不冒充专用显存。检测本身不下载模型或上传文件。

中文表格依据[官方本地部署说明](https://opendatalab.github.io/MinerU/quick_start/#local-deployment)，核对日期 2026-09-13。特别区分：CPU Pipeline 不要求独显；Engine 不支持纯 CPU；HTTP Client 的需求是客户端要求；Windows 主线 Python 为 3.10–3.12；评测分数来自 OmniDocBench v1.6，不是个人文档的准确率保证。官方表与应用的保守建议分开显示，“采用建议”只设置后端，不自动执行任务。

显存达标仍不保证模型可运行。驱动、PyTorch CUDA 构建及推理框架还需兼容；参见[官方 Windows FAQ](https://opendatalab.github.io/MinerU/faq/)和[扩展模块说明](https://opendatalab.github.io/MinerU/quick_start/extension_modules/)。内置参考不会自动随官网更新，界面提供官方原表链接。

## 打开与日常操作

双击 `启动 MinerU Desk.vbs`，或打开发布目录中的 `MinerU Desk.exe`。

1. 在转换工作台选择文件、文件夹或 URL，也可拖入本地文件。
2. 选择本机转换、官方云端或自建服务。
3. 选择输出文件夹、解析方式和识别选项，点击“开始转换”。
4. 在任务记录中查看状态和日志，完成后点击“查看”。
5. 结果页对照原文与 Markdown，切换“编辑”修正文字，点击“保存编辑”。

“指定文件夹”会被记住，每次新转换建立独立子目录。相同本地文件、参数、运行版本与输出根目录可复用已有结果。URL 的内容可能变化，不按 URL 字符串直接复用。重新转换会保留旧结果。

编辑版本保存为原 Markdown 文件旁的 `*.md.edited.md`，初始转换 Markdown 保留；再保存时保留 `*.previous` 上一编辑版本。并发编辑以修订号检查，发现冲突时须重新载入，避免覆盖另一方的修改。原始 Office 文件用系统应用打开，PDF 和图片支持页面内对照。

## 运行方式与输出

| 方式 | 文件与计算 | 主要配置 |
| --- | --- | --- |
| 本机转换 | 本机推理；首次使用可能下载模型 | Pipeline / Hybrid / VLM、OCR、公式、表格、页码、语言等 |
| 官方云端 | 文档上传 MinerU 官方服务 | 快速 / 精准 / 网页提取，Token，MD / JSON / HTML / LaTeX / DOCX |
| 自建服务 | 文档发送至指定服务 | MinerU API 地址；或 HTTP 推理后端及 OpenAI 兼容地址 |

本地支持 PDF、图片和 DOCX/PPTX/XLSX；旧版 DOC/PPT/XLS、HTML 与 URL 根据云端模式处理。实际功能以所安装的 MinerU / 服务版本为准。离线模式只使用已有本地模型。PDF 页码在界面中从 1 开始，本地后端只接受连续范围。云端精准解析可接受 `1-10,15`。

本地 MinerU 原生生成 Markdown、JSON、图片与中间结果，所以本地输出保留完整结果包。云端可选择额外导出格式。快速模式限制及图表能力在不同版本说明中存在差异，重要表格/公式宜选择精准解析并核验结果。

## 模型与服务

- 模型列表显示 Pipeline、VLM 模型组的配置路径与目录大小；Hybrid 同时使用两组模型。
- 可下载、补全/更新、停止下载，也可关联已有模型目录。
- 应用独立管理下载目录与 `mineru.json`；已有外部模型可以关联，默认由原来的工具管理删除。
- 删除按钮只对由本应用下载并记录的具体模型目录启用。删除前显示路径和大小，执行后不可从应用恢复；模型库共用的缓存文件可能仍然保留。
- 服务工具提供 API、Gradio、OpenAI 兼容推理服务与 Router 的启动、停止、日志和入口。专业参数用 JSON 数组填写，转交给对应的官方命令，不经过 shell 拼接。
- 安装版已带 Python 与 MinerU CPU 环境。源码版使用本地转换时，需关联已有 MinerU 或准备兼容的独立环境；云端 CLI 随 npm 依赖提供。

## 任务记录与 Codex

安装版的任务、配置和加密 Token 保存在外部工作区的 `data` 目录。源码开发模式默认使用 `%LOCALAPPDATA%\MinerU-Desk`。云端 Token 使用 Windows 当前用户加密保护。

桌面窗口关闭后，已启动的任务服务继续处理队列。重启电脑或服务后，未完成任务标记为“待继续”，由你点击继续；已完成任务保留。云端取消目前停止本地等待过程，不保证撤销服务器端已经提交的解析。

Codex 应通过应用的任务接口提交，才能与桌面共用记录；直接运行官方 `mineru` 或 `mineru-open-api` 的历史不会自动导入。

在应用源码目录运行：

```powershell
node cli.mjs status
node cli.mjs submit request.json
node cli.mjs task <任务ID>
node cli.mjs content <任务ID>
node cli.mjs retry <任务ID>
node cli.mjs cancel <任务ID>
node cli.mjs resume
node cli.mjs models
node cli.mjs edit <任务ID> edit-request.json
```

提交文件示例：

```json
{
  "files": ["D:/文献/论文.pdf"],
  "outputRoot": "D:/转换结果",
  "options": {
    "provider": "local",
    "backend": "pipeline",
    "method": "auto",
    "formats": ["md", "json"],
    "formula": true,
    "table": true
  }
}
```

编辑请求包括 `text` 和从 `content` 获取的 `revision`。服务未启动时先打开桌面应用，或运行 `npm run service`。通用接口调用：

```powershell
node cli.mjs request GET services
node cli.mjs request POST models/download download-request.json
```

本地服务监听 `127.0.0.1`，随机端口和访问凭据由 CLI 自动读取 `connection.json`。主要 API 包括 `state`、`tasks`、`tasks/:id/content`、`tasks/:id/edit`、`settings`、`models`、`models/download`、`models/delete`、`services/start` 和 `services/stop`。Codex 提交任务会标记来源为 `codex`。

## 分享与开发

给普通用户发送 Releases 中的 `MinerU-Desk-Setup-0.3.1-x64.exe`，其中包含程序、本地 CPU 依赖与云端 CLI，模型按需下载。不要发送自己使用过的工作区。`npm run pack` 仅打包桌面应用，不等同于包含 Python 运行环境的完整安装版；完整构建步骤见 [构建说明](packaging/构建安装包.md)。

```powershell
npm install
npm start
npm test
npm run smoke
npm run pack
```

真实转换回归测试：`node tests/real-smoke.mjs`，创建合成 DOCX 并调用已安装的本地 MinerU，检查转换、缓存复用和编辑保存。测试文件保存在 `test-output`，不属于应用发布包。

## 官方参考

- [MinerU 开源项目](https://github.com/opendatalab/MinerU)
- [本地命令与参数](https://opendatalab.github.io/MinerU/usage/cli_tools/)
- [模型来源与本地模型](https://opendatalab.github.io/MinerU/usage/model_source/)
- [官方云端 CLI / Skill](https://github.com/opendatalab/MinerU-Ecosystem)

本应用是个人使用的独立桌面界面，不是 MinerU 官方客户端。MinerU 程序和模型遵循各自许可证。
