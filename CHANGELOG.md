# 更新记录

## 0.3.1

- 修复随包 FastText 在 Windows 中文模型路径下加载失败的问题，保留原模型。
- 任务记录支持隐藏与恢复，也可以预览后清理转换资源；删除原始 PDF 需独立确认。
- 提供可选择安装路径的 Windows x64 安装程序，包含 MinerU 和 CPU 依赖，不包含 Pipeline/VLM 大模型。
- 程序目录与模型、缓存、结果工作区分开，卸载保留外部工作区。
- 提供 Codex 接入脚本，支持文件校验、环境诊断和共享任务 API；缺少 PDF 模型时返回 `needs-models`。

## 0.3.0

- 提供本地、云端和自建服务转换入口，以及 PDF / Markdown 对照与编辑。
- 支持模型路径设置、下载管理、存储清理、阶段进度和硬件检测建议。
- 桌面与 Codex 共用本地任务服务及记录。
