# 更新记录

## 0.3.3

- 关闭窗口时可选择最小化到托盘、退出或取消，并可记住选择。
- 托盘菜单支持恢复窗口、修改关闭行为和退出；点击最小化按钮默认收起到托盘，也可改为任务栏。
- 真正退出前检查运行中的任务和未保存内容，空闲时停止后台；更新和卸载不会被关闭询问拦住。

## 0.3.2

- 桌面程序、安装器和界面换用“笔和信纸”图标。
- 云端转换选项旁可直接打开官方 Token 申请页，或转到 Token 设置。
- 应用设置支持先检查新版本，查看版本与大小，再下载更新；校验 SHA-256 后由用户确认覆盖安装。
- 更新下载提供进度与取消操作，使用所选缓存目录。更新不要求先卸载，保留模型、原文、转换结果、设置与历史。
- 提供卸载入口和开始菜单卸载快捷方式，移除桌面程序及随包 MinerU / Python / CPU 依赖，保留外部工作区。
- 运行中任务、下载、服务及未保存编辑会阻止维护操作；安装器也检查旧程序是否已退出。

## 0.3.1

- 修复随包 FastText 在 Windows 中文模型路径下加载失败的问题，保留原模型。
- 任务记录支持隐藏与恢复，也可以预览后清理转换资源；删除原始 PDF 需独立确认。
- 提供可选择安装路径的 Windows x64 安装程序，包含 MinerU 和 CPU 依赖，不包含 Pipeline/VLM 大模型。
- 程序目录与模型、缓存、结果工作区分开，卸载保留外部工作区。
- 提供 Codex 接入脚本，支持文件校验、环境诊断和共享任务 API；缺少 PDF 模型时返回 `needs-models`。

## 0.3.0

- 提供本地、云端和自建服务转换入口，以及 PDF / Markdown 对照与编辑。
- 支持模型路径设置、下载管理、存储清理、阶段进度和硬件检测建议。
- 桌面与 Codex 共用本地任务服务及记录。
